{
  "name": "ribbit-kintone-plugins",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "postbuild": "tsx scripts/merge-outputs.ts",
    "dev": "turbo run dev",
    "lint": "biome lint .",
    "format": "biome format --write .",
    "check": "biome check --write .",
    "check-types": "turbo run check-types"
  },
  "devDependencies": {
    "@biomejs/biome": "catalog:",
    "@konomi-app/biome-config": "catalog:",
    "@types/node": "catalog:",
    "tailwindcss": "catalog:",
    "tsx": "catalog:",
    "turbo": "catalog:",
    "typesafe-i18n": "catalog:",
    "typescript": "catalog:"
  },
  "packageManager": "pnpm@11.22.0",
  "engines": {
    "node": ">=18"
  }
}
